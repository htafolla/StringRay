🚀 0xRay v1.22.50 — the governance release. 4 versions of work since v1.22.46.

🧠 Inference layer (1,600 lines): captures git patterns → generates proposals → agents vote before action
⚖️ Real governance: architect spawns code-reviewer, refactorer, researcher via task tool — they reject, abstain, and disagree (not rubber stamps)
📈 WeightedVotingAggregator: expertise × confidence × historical performance with time decay — learns from past votes
🛡️ Agent registry audit: removed 21 skill-only phantom entries — only real agents with .ts backing survive
🔧 6 naming bugs fixed (seoCopywriter→contentCreator, etc.) — agents now routeable by their real names
🗑️ Deprecated enforcer + orchestrator deleted — 45+ stale references cleaned across routing, delegation, metrics
✅ 2,2199 tests pass. Single-architect governance verified with real subagent votes.
```
npm install strray-ai@latest
```
The loop is closing: collect → propose → govern → verify → learn → repeat.
#0xRay #AIGovernance #SelfImproving #DevTools #NPM
