🎉 0xRay v1.22.46 is LIVE — AI governance that actually works.

🛡️ 60 codex terms block bad code before it ships — hallucinated APIs, missing error handling, unsafe patterns
🔍 Drop-in processors: add a file, governance auto-registers — extend without touching core
🧠 Self-reflecting: reads your git history, understands what changed and why, generates structured insights
🔁 Self-healing CI/CD: monitors deployments, classifies failures, auto-fixes what it's confident about, escalates the rest
🔌 Works everywhere — OpenCode, Hermes, OpenClaw, any MCP-compatible runtime
📊 2,2199 tests pass. 355 integration tests. Consumer E2E verified from npm.
```
npm install strray-ai@latest
```
What 0xRay is: guardrails for AI dev — codex compliance, structured routing, self-healing CI/CD via OpenCode/Hermes/OpenClaw.
#0xRay #AIOps #DevTools #SelfHealing #NPM
